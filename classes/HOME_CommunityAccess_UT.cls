/**
 * This class contains unit tests for validating the behavior of HOME_CommunityAccess
 * and triggers.
 */
@isTest
private class HOME_CommunityAccess_UT {
	
    /*
    static testMethod void getUrl() {
        HOME_CommunityAccess controller = new HOME_CommunityAccess();
        String url = controller.getURL();
        System.assert((url.contains('oauth_signature')), 'Invalid fastpass URL!');
        System.debug(url);
    }
    
    static testMethod void getLinkUrl() {
        HOME_CommunityAccess controller = new HOME_CommunityAccess();
        String url = controller.getLinkUrl();
        System.assert((url == 'https://www.servicemax.com/community.html'), 'Invalid community URL!');
        System.debug(url);
    }
    
    static testMethod void getLinkLabel() {
        HOME_CommunityAccess controller = new HOME_CommunityAccess();
        String label = controller.getLinkLabel();
        System.assert((label != null), 'Community link label cannot be empty!');
        System.debug(label);
    }
	*/
}